name: Build Installers

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-deb:
    name: Build Debian package
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.10"

      - name: Build .deb
        run: |
          chmod +x scripts/build-deb.sh
          ./scripts/build-deb.sh

      - name: Verify .deb health check
        run: |
          shopt -s nullglob
          deb_files=(dist/*.deb)
          if [ ${#deb_files[@]} -eq 0 ]; then
            echo "No .deb found in dist/ for verification."
            exit 1
          fi

          sudo apt-get update
          sudo apt-get install -y "./${deb_files[0]}"

          set +e
          health_output="$(nonet-movie --health 2>&1)"
          exit_code=$?
          set -e
          echo "$health_output"

          if [ "$exit_code" -ne 0 ]; then
            echo "nonet-movie --health exited with code $exit_code"
            exit 1
          fi

          if [[ "$health_output" != *"OK"* ]]; then
            echo "nonet-movie --health did not print OK"
            exit 1
          fi

      - name: Upload .deb artifact
        uses: actions/upload-artifact@v7
        with:
          name: nonet-movie-deb
          path: dist/*.deb
          if-no-files-found: error

  build-macos-app:
    name: Build macOS app bundle
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.10"

      - name: Build .app
        run: |
          chmod +x scripts/build-macos-app.sh
          ./scripts/build-macos-app.sh

      - name: Verify .app health check
        run: |
          shopt -s nullglob
          app_bundles=(dist/*.app)
          if [ ${#app_bundles[@]} -eq 0 ]; then
            echo "No .app found in dist/ for verification."
            exit 1
          fi

          set +e
          health_output="$("${app_bundles[0]}/Contents/MacOS/nonet-movie" --health 2>&1)"
          exit_code=$?
          set -e
          echo "$health_output"

          if [ "$exit_code" -ne 0 ]; then
            echo "nonet-movie --health exited with code $exit_code"
            exit 1
          fi

          if [[ "$health_output" != *"OK"* ]]; then
            echo "nonet-movie --health did not print OK"
            exit 1
          fi

      - name: Upload .app artifact
        uses: actions/upload-artifact@v7
        with:
          name: nonet-movie-macos-app
          path: |
            dist/*.app
            dist/*-macos-app.zip
          if-no-files-found: error

  build-windows-exe:
    name: Build Windows executable
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.10"

      - name: Build .exe
        shell: pwsh
        run: |
          ./scripts/build-windows-exe.ps1

      - name: Verify .exe health check
        shell: pwsh
        run: |
          $exe = Get-ChildItem -Path dist -Filter *.exe | Select-Object -First 1
          if (-not $exe) {
            throw "No .exe found in dist/ for verification."
          }

          $healthOutput = & $exe.FullName --health 2>&1
          $exitCode = $LASTEXITCODE
          $healthText = ($healthOutput | Out-String).Trim()
          Write-Host $healthText

          if ($exitCode -ne 0) {
            throw "Installer --health exited with code $exitCode."
          }

          if ($healthText -notmatch 'OK') {
            throw "Installer --health did not print OK."
          }

      - name: Upload .exe artifact
        uses: actions/upload-artifact@v7
        with:
          name: nonet-movie-windows-exe
          path: dist/*.exe
          if-no-files-found: error

  create-release:
    name: Create GitHub release
    runs-on: ubuntu-latest
    needs:
      - build-deb
      - build-macos-app
      - build-windows-exe
    if: startsWith(github.ref, 'refs/tags/v')

    steps:
      - name: Download .deb artifact
        uses: actions/download-artifact@v7
        with:
          name: nonet-movie-deb
          path: release-assets/deb

      - name: Download .app artifact
        uses: actions/download-artifact@v7
        with:
          name: nonet-movie-macos-app
          path: release-assets/app

      - name: Download .exe artifact
        uses: actions/download-artifact@v7
        with:
          name: nonet-movie-windows-exe
          path: release-assets/exe

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            release-assets/deb/*.deb
            release-assets/app/*-macos-app.zip
            release-assets/exe/*.exe
